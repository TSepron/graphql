type User {
    id: ID!
    firstName: String
    lastName: String
    password: String
    email: String!
}

type JWT {
    jwt: ID
}

type Query {
  user(id: ID!): User
}

type Mutation {
  loginUser(email: String!, password: String!): JWT
}